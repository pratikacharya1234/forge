{
  "task": "Implement JWT authentication middleware for Express",
  "results": {
    "gemini-2.5-flash": {
      "time_ms": 3500,
      "tokens": 420,
      "cost": 0.0,
      "code": "const jwt = require('jsonwebtoken');\n\nconst auth = (req, res, next) => {\n  const token = req.header('Authorization')?.replace('Bearer ', '');\n  if (!token) return res.status(401).send({ error: 'Access denied' });\n  try {\n    const decoded = jwt.verify(token, process.env.JWT_SECRET);\n    req.user = decoded;\n    next();\n  } catch (ex) {\n    res.status(400).send({ error: 'Invalid token' });\n  }\n};",
      "tool_calls": [
        { "step": 1, "action": "read_file", "content": "Checking package.json for jsonwebtoken..." },
        { "step": 2, "action": "write_file", "content": "Writing middleware/auth.js" }
      ]
    },
    "claude-4-sonnet": {
      "time_ms": 3200,
      "tokens": 390,
      "cost": 0.038,
      "code": "import jwt from 'jsonwebtoken';\n\nexport const authenticateToken = (req, res, next) => {\n  const authHeader = req.headers['authorization'];\n  const token = authHeader && authHeader.split(' ')[1];\n  if (token == null) return res.sendStatus(401);\n\n  jwt.verify(token, process.env.ACCESS_TOKEN_SECRET, (err, user) => {\n    if (err) return res.sendStatus(403);\n    req.user = user;\n    next();\n  });\n};",
      "tool_calls": [
        { "step": 1, "action": "search_files", "content": "Searching for existing auth patterns..." },
        { "step": 2, "action": "write_file", "content": "Creating auth middleware with ESM imports" }
      ]
    },
    "gpt-4.1": {
      "time_ms": 4800,
      "tokens": 510,
      "cost": 0.062,
      "code": "const passport = require('passport');\nconst { Strategy, ExtractJwt } = require('passport-jwt');\n\nconst opts = {\n  jwtFromRequest: ExtractJwt.fromAuthHeaderAsBearerToken(),\n  secretOrKey: process.env.SECRET_KEY\n};\n\npassport.use(new Strategy(opts, (jwt_payload, done) => {\n  User.findById(jwt_payload.id)\n    .then(user => done(null, user || false))\n    .catch(err => done(err, false));\n}));",
      "tool_calls": [
        { "step": 1, "action": "bash", "content": "npm install passport passport-jwt" },
        { "step": 2, "action": "write_file", "content": "Configuring Passport.js with JWT strategy" }
      ]
    }
  },
  "winner": "claude-4-sonnet",
  "winner_reason": "Cleanest implementation using modern ESM and better error handling."
}
