{
  "id": "auth-system",
  "task": "Implement OAuth2 PKCE flow for a TypeScript React application",
  "results": {
    "gemini-2.0-flash": {
      "time_ms": 5500,
      "tokens": 1200,
      "cost": 0.0006,
      "code": "const generateCodeChallenge = async (verifier: string) => {\n  const encoder = new TextEncoder();\n  const data = encoder.encode(verifier);\n  const digest = await window.crypto.subtle.digest('SHA-256', data);\n  return btoa(String.fromCharCode(...new Uint8Array(digest)))\n    .replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=+$/, '');\n};",
      "tool_calls": ["write_file", "search_files", "npm_test"]
    },
    "claude-3.5-sonnet": {
      "time_ms": 6200,
      "tokens": 1100,
      "cost": 0.0033,
      "code": "export const useAuth = () => {\n  const login = async () => {\n    const verifier = generateVerifier();\n    sessionStorage.setItem('verifier', verifier);\n    const challenge = await generateChallenge(verifier);\n    window.location.href = `${AUTH_URL}?code_challenge=${challenge}&code_challenge_method=S256`;\n  };\n  return { login };\n};",
      "tool_calls": ["write_file", "npm_test"]
    },
    "gpt-4o": {
      "time_ms": 4800,
      "tokens": 1350,
      "cost": 0.0067,
      "code": "function getAuthUrl(config) {\n  const verifier = crypto.randomBytes(32).toString('hex');\n  const challenge = crypto.createHash('sha256').update(verifier).digest('base64url');\n  return `${config.issuer}/authorize?` + new URLSearchParams({\n    client_id: config.clientId,\n    code_challenge: challenge,\n    code_challenge_method: 'S256'\n  });\n}",
      "tool_calls": ["write_file", "npm_test"]
    }
  },
  "winner": "gpt-4o"
}