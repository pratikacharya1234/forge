{
  "id": "data-pipeline",
  "task": "Refactor a Python data pipeline to use multiprocessing and handle OOM errors",
  "results": {
    "gemini-2.0-flash": {
      "time_ms": 4100,
      "tokens": 820,
      "cost": 0.0004,
      "code": "def process_batch(batch):\n    try:\n        return [transform(item) for item in batch]\n    except MemoryError:\n        return [transform(item) for item in split_batch(batch)]\n\nwith Pool(processes=os.cpu_count()) as pool:\n    for result in pool.imap_unordered(process_batch, chunked_data(large_dataset)):\n        save_result(result)",
      "tool_calls": ["read_file", "edit_file", "pytest"]
    },
    "claude-3.5-sonnet": {
      "time_ms": 3900,
      "tokens": 750,
      "cost": 0.0022,
      "code": "async def process_stream(it):\n    async for batch in aiostream.stream.chunks(it, 100):\n        tasks = [asyncio.create_task(transform(item)) for item in batch]\n        results = await asyncio.gather(*tasks, return_exceptions=True)\n        await save_results([r for r in results if not isinstance(r, Exception)])",
      "tool_calls": ["read_file", "edit_file", "pytest"]
    },
    "gpt-4o": {
      "time_ms": 5200,
      "tokens": 910,
      "cost": 0.0045,
      "code": "def run_pipeline(data):\n    with ProcessPoolExecutor() as executor:\n        futures = {executor.submit(process_item, item): item for item in data}\n        for future in as_completed(futures):\n            try:\n                yield future.result()\n            except Exception as exc:\n                logger.error(f'Item failed: {exc}')",
      "tool_calls": ["read_file", "edit_file", "pytest"]
    }
  },
  "winner": "claude-3.5-sonnet"
}